# 보안

- 이 저장소에는 비밀값이 없다. `.env` 는 커밋하지 않는다 (`.env.example` 만).
- 서빙 API 는 인증이 없다. 공개 배포 시 리버스 프록시(인증·속도 제한) 뒤에 둔다. 요청당 에이전트 수는 200 으로 제한된다(pydantic).
- 위치 데이터는 근로자 개인정보다. 실제 데이터로 운영하면 보존 기간·접근 통제·동의 절차가 필요하다 (`docs/model_card.md`).
- 취약점 제보: GitHub Security Advisory 또는 저장소 소유자에게 비공개로.
